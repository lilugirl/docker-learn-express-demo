version: "3"
services:
  express-app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./:/app:ro
      - /app/node_modules
    environment:
      - PORT=4000
    # env_file:
    #   - ./.env
